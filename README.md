# Ansible_Test_Article

idée de points à aborder :

- les tests sont réalisable avec Ansible via le module assert
- On peut verifier des configurations en verifiant des fichiers de configuration, un retour de commande, en utilisant des regex
- On peut record les result de test dans une variable
- Par la suite on peut utilser cette variable pour générer un rapport au format html via jinja2
